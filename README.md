# POS Inventory Starter


1. Copy files into project
2. Run `npm install`
3. Create `.env` from `.env.example` with your Firebase project values
4. Run `npm run dev`


Next steps:
- Add authentication (Firebase Auth)
- Wire up products collection and product pages
- Implement transactions for sale creation and inventory decrement
- Add tests and CI